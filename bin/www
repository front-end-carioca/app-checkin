#!/usr/bin/env node
var app = require('../app');
var port = process.env.PORT || 3000;

app.set('port', port);

app.listen(port, function() {
  console.log('App CheckIn rodando em: http://localhost:' + port);
});
